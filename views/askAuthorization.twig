<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>OAuth</title>
  <link rel="stylesheet" type="text/css" href="css/normalize.css">
  <link rel="stylesheet" type="text/css" href="css/default.css">
</head>
<body>
    <div id="container">

        {% if not sslEnabled %}
        <p class="warn">
            <strong>INSECURE</strong> connection to this service, TLS <strong>MUST</strong> be used!
            {% if contactEmail %}
                Please <a href="mailto:{{ contactEmail|e }}">inform</a> the application provider.
            {% endif %}
        </p>
        {% endif %}

        <h1 title="You are authenticated as '{{ resourceOwnerId|e }}'">My API</h1>

        <h3 title="{{ clientId|e }}">{{ clientName|e }}</h3>

        <blockquote>
            {{ clientDescription|e }}
        </blockquote>

        <p>
          This application wants the following permissions:
        </p>

        <ul>
          {% for scope in scopes %}
            <li><code>{{ scope|e }}</code></li>
          {% endfor %}
        </ul>

        <form class="askConfirmation" method="post" action="">
            <button class="err" type="submit" name="approval" value="reject">Reject</button>
            <button class="info" type="submit" name="approval" value="approve">Approve</button>
        </form>

        <div class="footer">
            php-oauth-as
        </div>

    </div> <!-- /container -->
</body>
</html>
